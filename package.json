{
  "name": "newamadeustest",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
 "scripts": {
  "pre-cy:run": "node PREPARE_DATASET.js",
  "cy:run": " npx cypress open --browser chrome  --e2e -e allure=true",
  "clean:folders": "rm -R -f allure-report/* && rm -R -f allure-results/*",
  "allure:report": "nohup npx allure generate allure-results --clean  -o allure-report && allure open allure-report --port 8080 --host 0.0.0.0",
  "kill:allure": "lsof -i :8080 | awk 'NR!=1 {print $2}' | xargs kill -9 || true",
  "ptest": "npm run clean:folders",
  "test": "npm run cy:run:headless || true",
  "posttest": "npm run kill:allure && npm run allure:report",
  "cy:run:headless": "cypress run --headless -e allure=true --record --key 0f1c2b1e-8b1e-4f0e-9b0e-1e8b1e4f0e9b --parallel"
}
,
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@shelex/cypress-allure-plugin": "^2.35.2",
    "allure-commandline": "^2.21.0",
    "cypress": "12.9.0",
    "moment": "^2.29.4",
    "xlsx": "^0.18.5"
  }
}
